require 'spec_helper'

describe PositiveSupport do
  it 'has a version number' do
    expect(PositiveSupport::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
